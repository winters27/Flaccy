version: '3.8'

services:
  flaccy:
    build: .
    container_name: flaccy
    env_file:
      - .env
    volumes:
      - ${DOWNLOAD_DIRECTORY}:/app/downloads
    ports:
      - "5001:5000"
    restart: unless-stopped
    user: "${UID}:${GID}"
